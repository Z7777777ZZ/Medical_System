# 第一小组详细架构设计

小组成员：周玥儿，林明哲，杨佳兰，周齐圣

---

## 1 功能模块

- 用户注册（医生、患者）
- 用户登陆（医生、患者）
- 患者健康信息
- 医生基本信息

---

## 2 技术栈

前端：Vue3+Element Plus

后端：Flask+SQLAlchemy

数据：MySQL(与主系统共享)

---

## 3 可参考的项目结构

```
medical-system/
├── backend/
│   ├── app.py                    # Flask 主程序  
│   ├── config.py                 # 配置文件
│   ├── extensions.py             # 扩展配置(SQLAlchemy, JWT等)
│   ├── requirements.txt          # Python 依赖
│   ├── user_service/
│   │   ├── __init__.py
│   │   ├── models/
│   │   │   ├── doctor.py         # 医生用户模型
│   │   │   └── patient.py        # 患者用户模型
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py           # 注册、登录相关API
│   │   │   ├── doctor.py         # 医生注册与信息管理API
│   │   │   └── patient.py        # 患者注册与信息管理API
│   │   ├── services/
│   │   │   ├── email_service.py  # 邮件服务（验证码） 可以用第五组的？
│   │   │   ├── sms_service.py    # 短信服务 可以用第五组的？
│   │   │   ├── profile_service.py # 用户信息管理服务
│   │   │   └── auth_service.py   # 用户认证与密码加密处理
│   │   └── utils/
│   │       ├── password.py       # 密码加密工具
│   │       └── token.py          # JWT令牌工具
│   └── migrations/               # 数据库迁移文件
├── frontend/ms_frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── auth/             # 认证相关组件
│   │   │   │   ├── LoginForm.vue # 登录表单组件
│   │   │   │   ├── RegisterForm.vue # 注册表单组件
│   │   │   │   ├── ProfileForm.vue # 个人信息表单
│   │   │   │   └── PasswordReset.vue # 密码重置表单
│   │   │   └── common/           # 通用组件
│   │   │       ├── Header.vue    # 页头组件
│   │   │       ├── Footer.vue    # 页脚组件
│   │   │       ├── Sidebar.vue   # 侧边栏组件
│   │   │       └── Dashboard.vue # 仪表盘组件
│   │   ├── views/
│   │   │   ├── auth/             # 认证相关页面
│   │   │   │   ├── LoginView.vue # 登录页面
│   │   │   │   ├── RegisterView.vue # 注册页面
│   │   │   │   ├── DoctorRegisterView.vue # 医生注册页面
│   │   │   │   ├── PatientRegisterView.vue # 患者注册页面
│   │   │   │   └── ProfileView.vue # 个人信息页面
│   │   │   ├── HomeView.vue       # 主页
│   │   │   └─── DashboardView.vue # 仪表盘页面
│   │   ├── router/               # 路由配置
│   │   │   └── index.js          # 路由定义和权限控制
│   │   ├── store/
│   │   │   ├── auth.js           # 认证状态管理
│   │   │   └── user.js           # 用户状态管理
│   │   ├── api/
│   │   │   ├── auth_service.js   # 认证服务API调用
│   │   │   └── user_service.js   # 用户服务API调用
│   │   ├── utils/                # 工具函数
│   │   │   ├── auth.js           # 认证相关工具函数
│   │   │   └── request.js        # 请求拦截器和通用处理
│   │   ├── api/
│   │   │   └── user_service.js  # 用户服务API调用
│   │   └── assets/
│   │       └── images/          # 引导和UI相关图片
│   ├── package.json             # 前端依赖
│   └── tailwind.config.js       # TailwindCSS配置
└── database/
    └── user_service_tables.sql  # 用户服务相关表结构
```

## 4 数据库额外设计

在原有数据的基础上，添加以下表结构

#### 4.1 用户表（patients）

| 字段名        | 类型         | 说明       |
| ------------- | ------------ | ---------- |
| patient_id    | BIGINT       | 主键，自增 |
| phone         | VARCHAR(20)  | 手机号     |
| email         | VARCHAR(100) | 邮箱       |
| name          | VARCHAR(50)  | 姓名       |
| password_hash | VARCHAR(255) | 密码哈希   |
| created_at    | DATETIME     | 创建时间   |

#### 4.2 医生表（doctors）

| 字段名        | 类型         | 说明             |
| ------------- | ------------ | ---------------- |
| doctor_id     | BIGINT       | 主键，自增       |
| phone         | VARCHAR(20)  | 手机号           |
| name          | VARCHAR(50)  | 姓名             |
| hospital_id   | BIGINT       | 外键，关联医院表 |
| department_id | BIGINT       | 外键，关联科室表 |
| specialty     | VARCHAR(100) | 专长             |
| bio           | TEXT         | 个人简介         |
| password_hash | VARCHAR(255) | 密码哈希         |
| created_at    | DATETIME     | 创建时间         |

#### 4.3 医院表（hospitals）(注意：这里医院表可以暂时只有一条数据，后期功能扩展可以有多个医院)

| 字段名      | 类型         | 说明       |
| ----------- | ------------ | ---------- |
| hospital_id | BIGINT       | 主键，自增 |
| name        | VARCHAR(100) | 医院名称   |
| address     | VARCHAR(255) | 地址       |

#### 4.4 科室表（departments）

| 字段名        | 类型        | 说明             |
| ------------- | ----------- | ---------------- |
| department_id | BIGINT      | 主键，自增       |
| name          | VARCHAR(50) | 科室名称         |
| hospital_id   | BIGINT      | 外键，关联医院表 |

#### 4.5 患者信息表（patient_details）

| 字段名               | 数据类型                                               | 约束                     | 描述           |
| -------------------- | ------------------------------------------------------ | ------------------------ | -------------- |
| patient_id           | BIGINT                                                 | PRIMARY KEY, FOREIGN KEY | 关联用户ID     |
| gender               | ENUM('male', 'female', 'other')                        | NULL                     | 性别           |
| date_of_birth        | DATE                                                   | NULL                     | 出生日期       |
| blood_type           | ENUM('A+', 'A-', 'B+', 'B-', 'AB+', 'AB-', 'O+', 'O-') | NULL                     | 血型           |
| height               | DECIMAL(5,2)                                           | NULL                     | 身高(cm)       |
| weight               | DECIMAL(5,2)                                           | NULL                     | 体重(kg)       |
| emergency_contact    | VARCHAR(100)                                           | NULL                     | 紧急联系人姓名 |
| emergency_phone      | VARCHAR(20)                                            | NULL                     | 紧急联系人电话 |
| medical_insurance_id | VARCHAR(50)                                            | NULL                     | 医保卡号       |
| allergies            | TEXT                                                   | NULL                     | 过敏史         |
| chronic_conditions   | TEXT                                                   | NULL                     | 慢性病史       |
| medications          | TEXT                                                   | NULL                     | 当前用药情况   |

```SQL
-- 创建数据库
CREATE DATABASE IF NOT EXISTS medical_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE medical_system;

-- 创建医院表 (hospitals)
CREATE TABLE IF NOT EXISTS hospitals (
    hospital_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    address VARCHAR(255) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 创建科室表 (departments)
CREATE TABLE IF NOT EXISTS departments (
    department_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    hospital_id BIGINT NOT NULL,
    FOREIGN KEY (hospital_id) REFERENCES hospitals(hospital_id) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 创建患者表 (patients)
CREATE TABLE IF NOT EXISTS patients (
    patient_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    name VARCHAR(50) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 创建医生表 (doctors)
CREATE TABLE IF NOT EXISTS doctors (
    doctor_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) UNIQUE NOT NULL,
    name VARCHAR(50) NOT NULL,
    hospital_id BIGINT NOT NULL,
    department_id BIGINT NOT NULL,
    specialty VARCHAR(100) NOT NULL,
    bio TEXT,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (hospital_id) REFERENCES hospitals(hospital_id) ON DELETE RESTRICT ON UPDATE CASCADE,
    FOREIGN KEY (department_id) REFERENCES departments(department_id) ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 创建患者详情表 (patient_details)
CREATE TABLE IF NOT EXISTS patient_details (
    patient_id BIGINT PRIMARY KEY,
    gender ENUM('male', 'female', 'other') NULL,
    date_of_birth DATE NULL,
    blood_type ENUM('A+', 'A-', 'B+', 'B-', 'AB+', 'AB-', 'O+', 'O-') NULL,
    height DECIMAL(5,2) NULL,
    weight DECIMAL(5,2) NULL,
    emergency_contact VARCHAR(100) NULL,
    emergency_phone VARCHAR(20) NULL,
    medical_insurance_id VARCHAR(50) NULL,
    allergies TEXT NULL,
    chronic_conditions TEXT NULL,
    medications TEXT NULL,
    FOREIGN KEY (patient_id) REFERENCES patients(patient_id) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

```

```mysql
-- 插入默认医院数据
INSERT INTO hospitals (name, address) 
VALUES ('智慧医疗中心医院', '北京市海淀区中关村南大街5号');

-- 插入默认科室数据
INSERT INTO departments (name, hospital_id) VALUES 
('内科', 1),
('外科', 1),
('儿科', 1),
('妇产科', 1),
('眼科', 1),
('耳鼻喉科', 1),
('口腔科', 1),
('皮肤科', 1),
('神经内科', 1),
('心血管内科', 1),
('呼吸内科', 1),
('消化内科', 1),
('肾内科', 1),
('内分泌科', 1),
('血液科', 1),
('肿瘤科', 1),
('骨科', 1),
('康复科', 1),
('急诊科', 1),
('精神心理科', 1);

-- 为数据库添加索引
CREATE INDEX idx_patients_phone ON patients(phone);
CREATE INDEX idx_patients_email ON patients(email);
CREATE INDEX idx_doctors_phone ON doctors(phone);
CREATE INDEX idx_doctors_dept ON doctors(department_id);
CREATE INDEX idx_doctors_hosp ON doctors(hospital_id);

-- 创建视图：医生与科室信息
CREATE VIEW doctor_department_view AS
SELECT 
    d.doctor_id,
    d.name AS doctor_name,
    d.phone,
    d.specialty,
    dept.name AS department_name,
    h.name AS hospital_name
FROM 
    doctors d
JOIN 
    departments dept ON d.department_id = dept.department_id
JOIN 
    hospitals h ON d.hospital_id = h.hospital_id;
```



---

## 5 子系统API设计

```json
{
  "status": "success" | "error",
  "message": "描述性消息",
  "data": { /* 具体数据对象 */ }
}
```

#### 5.1 用户端API

##### 5.1.1 用户注册

- **方法**：`POST`
- **路径**：`/api/patients/register`
- **请求参数**：

```JSON
{
  "phone": "13800138000",
  "email": "user@example.com",
  "password": "password123",
  "name": "张三"
}
```

- **响应**：

```JSON
{
  "status": "success",
  "message": "注册成功",
  "data": {
    "patient_id": 1,
    "token": "jwt_token_here"
  }
}
```

##### 5.1.2 用户登录

- **方法**：`POST`
- **路径**：`/api/patients/login`
- **请求参数**：

```JSON
{
  "phone": "13800138000",
  "password": "password123"
}
```

- **响应**：

```JSON
{
  "status": "success",
  "message": "登录成功",
  "data": {
    "token": "jwt_token_here",
    "patient_id": 1
  }
}
```

##### 5.1.3 获取个人信息

- **方法**：`GET`
- **路径**：`/api/patients/{patient_id}/profile`
- **响应**：

```JSON
{
  "status": "success",
  "message": "获取成功",
  "data": {
    "patient_id": 1,
    "phone": "13800138000",
    "email": "user@example.com",
    "name": "张三",
    "patient_details": {
        "gender": "string",  
        "date_of_birth": "date",  
        "blood_type": "string",  
        "allergies": "string",  
        "chronic_conditions": "string"
    }
  }
}
```

##### 5.1.4 更新患者信息

- **方法**：`PUT`
- **路径**：`/api/patients/{patient_id}/``patient_details`
- **请求参数**：

```json
{
  "allergies": "花粉",
  "chronic_diseases": "高血压"
}
```

- **响应**：

```json
{
  "status": "success",
  "message": "更新成功",
  "data": null
}
```

#### 5.2 医生端API

##### 5.2.1 医生注册

- **方法**：`POST`
- **路径**：`/api/doctors/register`
- **请求参数**：

```json
{
  "phone": "13900139000",
  "name": "李医生",
  "hospital_id": 1,
  "department_id": 1,
  "specialty": "内科",
  "password": "docpass123"
}
```

- **响应**：

```json
{
  "status": "success",
  "message": "注册成功",
  "data": {
    "doctor_id": 1
  }
}
```

##### 5.2.2 医生登录

- **方法**：`POST`
- **路径**：`/api/doctors/login`
- **请求参数**：

```json
{
  "phone": "13900139000",
  "password": "docpass123"
}
```

- **响应**：

```json
{
  "status": "success",
  "message": "登录成功",
  "data": {
    "token": "jwt_token_here",
    "doctor_id": 1
  }
}
```

##### 5.2.3 获取医生个人信息

- **方法**：`GET`
- **路径**：`/api/doctors/{doctor_id}/profile`
- **响应**：

```json
{
  "status": "success",
  "message": "获取成功",
  "data": {
    "doctor_id": 1,
    "phone": "13900139000",
    "name": "李医生",
    "hospital": "人民医院",
    "department": "内科",
    "specialty": "内科",
    "bio": "10年经验"
  }
}
```