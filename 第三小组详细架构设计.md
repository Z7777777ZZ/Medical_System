# 第三小组详细架构设计

小组成员：陈宏哲，武煜迪，杨培源，尹松屹

---

## 1 功能模块

### 1.1 智能叫号管理模块
- **实时队列监控**：可视化展示当前候诊队列状态
- **智能叫号策略**：支持按预约时间/紧急程度/科室优先级排序（这里要对应的数据库数据）
- **多终端同步**：医院端/医生端/患者端三端数据（可以使用类继承的方式）
- **异常处理**：叫号未到自动顺延（提供一个交互的按钮）
- **数据看板**：候诊时长统计、医生接诊效率分析

### 1.2 诊疗核心模块
- **结构化病历录入**：支持症状模板，检查模板等快速录入（类似 ppt 的模板框架）
- **智能处方系统**：
  - 药品冲突检测（这个感觉不好做，药品数据库里需要加一条互斥信息）
  - 剂量自动换算（根据体重/年龄自动计算合理剂量，使用一个数学公式意思意思就行）
- **多模态病历管理**：
  - 支持图文混排（检查报告图片嵌入）
  - 病程时间轴展示
- **诊疗闭环管理**：问诊→检查→诊断→处方全流程关联

---

## 2 技术栈

独立的部分想用啥用啥，前端 Vue3，后端 Flask 就行。

---

## 3 可参考的项目结构

```
medical-system/
├── backend/
│   ├── app.py                    # Flask 主程序  
│   ├── config.py                 # 配置文件
│   ├── extensions.py             # 扩展配置
│   ├── requirements.txt          # Python 依赖
│   ├── call_number/              # 叫号管理模块
│   │   ├── __init__.py
│   │   ├── models/
│   │   │   ├── queue.py          # 排队信息模型
│   │   │   └── call_log.py       # 叫号记录模型
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── queue.py          # 排队信息 API
│   │   │   └── call.py           # 叫号操作 API
│   │   └── services/
│   │       ├── queue_service.py  # 排队服务
│   │       └── call_service.py   # 叫号服务
│   ├── diagnosis/
│   │   ├── __init__.py
│   │   ├── models/
│   │   │   ├── diagnosis_record.py # 诊断记录模型，写一下症状的一下模板
│   │   │   ├── prescription.py   # 处方模型，药方的格式
│   │   │   └── medical_record.py # 病历模型，层次在诊断记录和处方之上
│   │   ├── api/ # 这部分是和前端的交互逻辑
│   │   │   ├── __init__.py
│   │   │   ├── diagnosis_record.py # 诊断记录 API
│   │   │   ├── prescription.py   # 处方 API
│   │   │   └── medical_record.py # 病历 API
│   │   ├── services/
│   │   │   ├── diagnosis_service.py # 诊断服务，使用诊断模型来工作
│   │   │   ├── prescription_service.py # 处方服务，使用处方模型来工作
│   │   │   └── record_edit.py     # 还是相对另外两个层次更高，但是其它两个也要能够直接访问
│   └── utils/
│   │   └── validators.py     # 数据验证工具，类似 md5sum 进行数据校验？感觉没什么用（
│   └── migrations/               # 数据库迁移文件，我也不知道干什么的，抄别的组的
├── frontend/ms_frontend/	# 略

└── database/
    └── diagnosis_tables.sql     # 诊断与治疗相关表结构
```

## 4 数据库额外设计

这里就给出一下表格形式的

#### 4.1 电子病历表（medical_records）

| 字段名          | 类型     | 说明                                                         |
| --------------- | -------- | ------------------------------------------------------------ |
| record_id       | BIGINT   | 主键，自增                                                   |
| patient_id      | BIGINT   | 外键，关联用户表                                             |
| doctor_id       | BIGINT   | 外键，关联医生表                                             |
| visit_date      | DATE     | 就诊日期                                                     |
| discription     | TEXT     | 患者自述                                                     |
| photo           | TEXT     | 有关检查的照片，存储上传到图床的图片的 URL，多个图片用回车区分 |
| diagnosis       | TEXT     | 诊断结果                                                     |
| prescription_id | BIGINT   | 外键，关联处方表                                             |
| treatment       | TEXT     | 治疗方案                                                     |
| created_at      | DATETIME | 创建时间                                                     |

#### 4.7 预约表（appointments）

感觉叫号和预约应该是要一起的吧

| 字段名           | 类型     | 说明                           |
| ---------------- | -------- | ------------------------------ |
| appointment_id   | BIGINT   | 主键，自增                     |
| patient_id       | BIGINT   | 外键，关联用户表               |
| doctor_id        | BIGINT   | 外键，关联医生表               |
| appointment_time | DATETIME | 预约时间                       |
| status           | ENUM     | 状态（待确认、已确认、已取消） |

#### 4.9 排队表（queues）

| 字段名       | 类型   | 说明                   |
| ------------ | ------ | ---------------------- |
| queue_id     | BIGINT | 主键，自增             |
| patient_id   | BIGINT | 外键，关联用户表       |
| doctor_id    | BIGINT | 外键，关联医生表       |
| queue_number | INT    | 排队号码               |
| status       | ENUM   | 状态（等待中、已叫号） |

#### 4.10 药品表（medicines）

| 字段名      | 类型          | 说明       |
| ----------- | ------------- | ---------- |
| medicine_id | BIGINT        | 主键，自增 |
| name        | VARCHAR(100)  | 药品名称   |
| price       | DECIMAL(10,2) | 价格       |
| description | TEXT          | 药品描述   |

#### 4.11 处方表（prescriptions）

| 字段名          | 类型     | 说明             |
| --------------- | -------- | ---------------- |
| prescription_id | BIGINT   | 主键，自增       |
| patient_id      | BIGINT   | 外键，关联用户表 |
| doctor_id       | BIGINT   | 外键，关联医生表 |
| created_at      | DATETIME | 创建时间         |

#### 4.12 处方明细表（prescription_details）

| 字段名          | 类型   | 说明             |
| --------------- | ------ | ---------------- |
| detail_id       | BIGINT | 主键，自增       |
| prescription_id | BIGINT | 外键，关联处方表 |
| medicine_id     | BIGINT | 外键，关联药品表 |
| instructions    | TEXT   | 用药指导         |

---

## 5 子系统API设计

无非就是要什么，给什么，感觉这个边写边加会好一点。